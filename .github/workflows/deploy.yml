# Deploy static content to GitHub Pages (same pattern as timestables)
name: Deploy static content to Pages

on:
  push:
    branches: ['main']
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: 'pages'
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup ImageMagick
        uses: mfinelli/setup-imagemagick@v7
      - name: Prepare deployment
        run: |
          mkdir -p deploy/icons
          cp music-timer.html deploy/index.html
          cp manifest.json deploy/
          cp sw.js deploy/
          magick -size 192x192 xc:'#1a1a2e' deploy/icons/icon-192.png
          magick -size 512x512 xc:'#1a1a2e' deploy/icons/icon-512.png
      - name: Setup Pages
        uses: actions/configure-pages@v3
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './deploy'
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
